#Configuration file for MyTag v1.1.0
enable:
 auto-set: true
 #Set to true to set the tag to the player's preferences on-join
first-tag: "{player}"
#Default tag the player will have on their first join
op-tag: "Staff"
#Custom tag for /tag op, example: [Admin] Steve
preferred-economy: 
#Will be used for /tag money
version: 1.1.0
#Do not change the version, it is used for internal purposes
